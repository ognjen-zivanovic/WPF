<UserControl x:Class="WpfApp1.RoomCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp1"
             Height="Auto" Width="750" Margin="10">
    <Border BorderBrush="Gray" BorderThickness="1" Background="#F0F8FF" CornerRadius="10">
        <StackPanel Margin="10">
            <Grid Margin="0 0 0 10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Image x:Name="RoomImage" Stretch="UniformToFill" Grid.Row="0" Height="200"/>
                <StackPanel x:Name="AmenityPanel" Orientation="Horizontal" 
                            Grid.Row="1" HorizontalAlignment="Center" 
                            Background="#80000000" Margin="0,5,0,0"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="CapacityText" FontSize="16" Margin="0,0,0,5"/>
                    <TextBlock x:Name="PriceText" FontSize="24" FontWeight="Bold" Foreground="DarkGreen"/>
                </StackPanel>
                <Button Content="Book Room" Width="100" Height="30" 
                        Background="#ADD8E6" HorizontalAlignment="Right" 
                        Margin="20,10,0,0" Click="BookRoom_Click"
                        Grid.Column="1"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
